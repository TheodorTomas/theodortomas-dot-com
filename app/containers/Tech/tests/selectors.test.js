// import { fromJS } from 'immutable';
// import { makeSelectTechDomain } from '../selectors';

// const selector = makeSelectTechDomain();

describe('makeSelectTechDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
