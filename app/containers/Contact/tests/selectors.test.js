// import { fromJS } from 'immutable';
// import { makeSelectContactDomain } from '../selectors';

// const selector = makeSelectContactDomain();

describe('makeSelectContactDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
