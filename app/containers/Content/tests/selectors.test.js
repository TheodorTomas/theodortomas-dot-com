// import { fromJS } from 'immutable';
// import { makeSelectContentDomain } from '../selectors';

// const selector = makeSelectContentDomain();

describe('makeSelectContentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
