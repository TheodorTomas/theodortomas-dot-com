// import { fromJS } from 'immutable';
// import { makeSelectHomeDomain } from '../selectors';

// const selector = makeSelectHomeDomain();

describe('makeSelectHomeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
